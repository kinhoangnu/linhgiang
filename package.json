{
  "name": "linhgiang",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/run-playwright.mjs",
    "emulators": "npx firebase-tools emulators:start --only auth,firestore,hosting",
    "deploy:hosting": "npx firebase-tools deploy --only hosting",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules,firestore:indexes"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "@vitejs/plugin-react": "^6.0.0",
    "vite": "^8.0.10"
  }
}
