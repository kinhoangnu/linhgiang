interface:
  display_name: "End Session"
  short_description: "Validate, commit, and push."
  default_prompt: "Use $endsession to review Linhgiang changes, validate, update continuity docs, commit, and push main."
policy:
  allow_implicit_invocation: true
